#!/bin/zsh

autoload zmv
alias mmv='noglob zmv -W'

# For giggles
alias du="du -hcs"
alias df="df -h"

alias ls="ls --color=auto"
alias la="ls --color=always -a"
alias ll="ls --color=always -l"
alias lla="ls --color=always -la"
alias lal="ls --color=always -la"

alias cp="cp -i"

#rsa stuff
alias ssh="loadrsa; ssh"
alias scp="loadrsa; scp"
alias git="loadrsa; git"
alias hg="loadrsa; hg"

export LANG="en_US.utf8"

alias grep="grep --colour=auto"

alias python="/usr/bin/python2"
alias ipython="/usr/bin/ipython2"

vik(){
       sed -i $1d ~/.ssh/known_hosts
}

ssh2(){
        cat ~/.ssh/id_rsa.pub | /usr/bin/ssh $1 'umask 077; test -d .ssh || mkdir .ssh; cat > /tmp/mykey && grep "`cat /tmp/mykey`" ~/.ssh/authorized_keys &> /dev/null || cat /tmp/mykey >> ~/.ssh/authorized_keys' || return 1
            /usr/bin/ssh $1
}

loadkey(){
    eval `keychain -q --eval --agents ssh id_rsa`
}

# grep for the text in the following directory or the sub directories                               
# and search for folders having the same name / for actions in pylabs 
function v(){
    vimbin="vim"
    if [ -e "/usr/bin/gvim" ]; then
        vimbin="gvim"
    fi
    if [ -z "$@" ] ; then
        $vimbin -c "normal '0";
    else
        $vimbin `echo "$@" | cut -d: -f1,2 | sed -e 's/:/ +/g'`;
    fi
}

