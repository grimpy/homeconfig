if [ ! -e /tmp/scrolllock ];then
	touch /tmp/scrolllock
	while [ -e /tmp/scrolllock ]; do
        sudo su -c 'setleds -D -caps < /dev/console'
		sleep 0.5
        sudo su -c 'setleds -D +caps < /dev/console'
		sleep 0.5 
	done
    sudo su -c 'setleds -D -caps < /dev/console'
fi
