#!/usr/bin/env python
import os, sys

txtfile = '/home/grimpy/Desktop/irssi.txt'
f = open(txtfile, 'a')
parts = sys.argv[1].split('/')
f.write('/server %s\n' % parts[2])
f.write('/join %s\n\n' % parts[3])
f.close()
os.system('geany %s' % txtfile)
